# Sanity CMS
NEXT_PUBLIC_SANITY_PROJECT_ID="your-project-id"
NEXT_PUBLIC_SANITY_DATASET="production"

# Contact form — Resend (primario)
RESEND_API_KEY="re_..."
NEXT_PUBLIC_CONTACT_EMAIL="info@carpenteriapastro.it"

# Contact form — Nodemailer fallback (opzionale)
# Se Resend fallisce, prova SMTP custom
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
